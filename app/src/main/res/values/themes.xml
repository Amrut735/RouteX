<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme – actual theming is handled in Compose -->
    <style name="Theme.RouteX" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash screen theme using androidx.core:core-splashscreen -->
    <style name="Theme.RouteX.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_background</item>
        <item name="postSplashScreenTheme">@style/Theme.RouteX</item>
    </style>
</resources>
